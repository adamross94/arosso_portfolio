/* Imports the SectionWrapper component from a module and immediately re-exports it, 
allowing other parts of the application to easily access and use the SectionWrapper 
through this module. This can be useful for organizing and abstracting components 
into separate files for better code organization and modularity. */ 

import SectionWrapper from "./SectionWrapper";

export { SectionWrapper };